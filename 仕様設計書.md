# 仕様設計書：LightGBM によるレースタイム予測（回帰）

## 1. 目的
- `Data/df.csv` を用いて **レースタイムの回帰予測** を実験する。
- 実装前に、データ仕様・前処理・評価設計を明確化する。
  - 本書は **実装済みの内容**（スクリプト・出力物・ログ）も併記する。

## 2. 対象データの概要（現状確認）
- 入力: `Data/df.csv`
- 行数: **3,327,756 行**（ヘッダ除く）
- 列数: **58 列**
- 期間: **2014-01-01 〜 2024-11-09**（`date` 列）
- 粒度: **1行 = 1レース内の1艇**
  - `race_id` は同一レースで共有（約 554,626 レース）

### 2.1 目的変数（ターゲット）
- 既定ターゲット: `レースタイム_refix`（float）
  - 欠損: **389 行**
  - 値域: **1108 〜 3167**、平均 **1544.73**
  - `レースタイム` から `'`/`"` を除去して数値化されたもの
  - 例: **1分54.4秒 → 1544**（分・秒が連続値になっていない）
- `レースタイム`（object）は欠損が多いため（**809,780 行欠損**）本実験では使用しない

## 3. 特徴量設計（リーク対策含む）

### 3.1 使用する列（前提: 予測時点はレース前）
- レース/環境: `ET`, `tilt`, `EST`, `ESC`, `date`, `place`, `race_no`, `weather`,
  `air_t`, `wind_d`, `wind_v`, `water_t`, `wave_h`
- 枠・選手: `枠`, `F`, `L`, `age`, `weight`, `racer_no`, `sibu`, `born`
- 勝率系: `zwin_r_1`, `zwin_r_2`, `zwin_r_3`, `twin_r_1`, `twin_r_2`, `twin_r_3`
- 機体・部品: `moter_no`, `mwin_r_2`, `mwin_r_3`, `boat_no`, `bwin_r_2`, `bwin_r_3`,
  `piston`, `ring`, `electric`, `carburetor`, `cylinder`, `shafts`, `gears`, `carrier`, `propera`
- 追加特徴量: `racer_lank_int`, `lank_counts`,
  `ET_std`, `zwin_r_1_std`, `zwin_r_2_std`, `zwin_r_3_std`, `twin_r_1_std`, `mwin_r_2_std`, `bwin_r_2_std`
- 日付派生（任意）: `date` から `year`, `month`, `dayofweek` を作成

### 3.2 除外列（リーク・識別子）
- 識別子: `race_id`
- 事後情報（レース結果依存）: `着`, `決まり手`, `PST`, `TSC`
- 目的変数: `レースタイム`, `レースタイム_refix`

> 補足: 事後情報を含めた学習は実運用ではリークとなるため、
> 本実験は **レース前特徴量のみ** の設定を基準とする。

## 4. 前処理方針
- `date` を `datetime` に変換
- 目的変数の連続化（重要）
  - `レースタイム_refix` は **分・秒が連続値になっていない** ため、学習前に秒へ変換する
  - 変換例（1分54.4秒 → 1544 の場合）:
    - `minutes = floor(rt / 1000)`
    - `seconds = (rt % 1000) / 10`
    - `rt_sec = minutes * 60 + seconds`
  - 学習・評価は **`target_sec`** を使用（必要なら予測後に元表記へ戻す）
- 欠損処理
  - 目的変数が欠損の行は除外
  - 説明変数の欠損は LightGBM の欠損処理を利用（原則補完しない）
- 型変換
  - **カテゴリとして扱う候補**: `place`, `race_no`, `ESC`, `枠`, `racer_no`, `moter_no`, `boat_no`,
    `sibu`, `born`, `piston`, `ring`, `electric`, `carburetor`, `cylinder`, `shafts`, `gears`, `carrier`, `propera`
  - 上記は `category` 型に変換し、LightGBM の `categorical_feature` に指定

## 5. 分割・検証設計
- **時系列分割を優先**（日付で昇順ソート）
- 同一 `race_id` が train/valid/test に混在しないように **レース単位で分割**

### 5.1 推奨ホールドアウト（例）
- Train: 2014-01-01 〜 2022-12-31
- Valid: 2023-01-01 〜 2023-12-31
- Test : 2024-01-01 〜 2024-11-09

### 5.2 時系列CV（任意）
- Expanding window で 4〜5 fold
- シャッフルなし

## 6. モデル設計（LightGBM）
- モデル: `LGBMRegressor`
- 目的関数: `regression`
- 評価指標: `rmse`（主指標）, `mae`, `r2`

### 6.1 初期ハイパーパラメータ（案）
- `n_estimators`: 2000
- `learning_rate`: 0.05
- `num_leaves`: 63
- `max_depth`: -1
- `min_data_in_leaf`: 50
- `subsample`: 0.8
- `colsample_bytree`: 0.8
- `random_state`: 42
- `n_jobs`: -1
- 早期終了: `early_stopping_rounds=200`（valid を使用）

## 7. 評価とレポート
- 指標: RMSE / MAE / R2
- 追加: 期間別（月次/年次）での誤差推移を確認
- 重要度: `feature_importances_` を保存

## 8. 中間成果物（動作確認用）
※ **重くならない**範囲で「情報量が多い」成果物を出力する。原則は **集計・サマリ中心**。

### 8.1 データ全体サマリ（前処理後）
- `artifacts/lgbm_racetime/intermediate/dataset_overview.json`
  - 行数/列数/期間/目的変数欠損数など
- `artifacts/lgbm_racetime/intermediate/missing_summary.csv`
  - 列ごとの欠損数・欠損率
- `artifacts/lgbm_racetime/intermediate/categorical_cardinality.csv`
  - カテゴリ列のユニーク数（高カーディナリティ検出）
- `artifacts/lgbm_racetime/intermediate/feature_summary.csv`
  - 数値列の min/max/mean/std/percentile（5, 50, 95）

### 8.2 目的変数の分布チェック
- `artifacts/lgbm_racetime/intermediate/target_hist.csv`
  - ビン別件数（ヒストグラム集計）
- `artifacts/lgbm_racetime/intermediate/target_by_month.csv`
  - 月次の mean/median/std/件数
- （任意）`target_hist.png` / `target_by_month.png`
  - 画像は軽量 PNG で出力

### 8.3 分割の妥当性チェック
- `artifacts/lgbm_racetime/intermediate/split_summary.csv`
  - train/valid/test の件数・ユニーク `race_id` 数
- `artifacts/lgbm_racetime/intermediate/split_date_ranges.json`
  - 各 split の日付レンジ
- `artifacts/lgbm_racetime/intermediate/split_overlap_check.txt`
  - split 間の `race_id` 交差件数（0 であることを確認）

### 8.4 サンプル出力（軽量）
- `artifacts/lgbm_racetime/intermediate/sample_rows.csv`
  - ランダム 10,000 行（seed 固定）
  - 目視確認用

## 9. ログ設計
### 9.1 出力先
- `artifacts/lgbm_racetime/logs/run_YYYYmmdd_HHMMSS.log`
- 標準出力にも同内容を出力

### 9.2 ログ形式
- 形式: **JSONL**（1行=1レコード）
- 例キー: `ts`, `level`, `event`, `message`, `data`
- レベル: `INFO` / `WARNING` / `ERROR`（`DEBUG` は任意）

### 9.3 記録内容（最低限）
- 実行開始/終了、実行時間
- 主要設定（学習パラメータ、分割期間、seed）
- データ読み込み結果（行数・列数）
- 欠損除外・前処理での削除件数
- split 件数・`race_id` の重複チェック結果
- 学習開始/終了、best iteration、評価指標
- 中間成果物の出力先一覧

## 10. 成果物（想定）
- `artifacts/lgbm_racetime/`
  - `model.txt` or `model.pkl`
  - `metrics.json`
  - `feature_importance.csv`
  - `config.json`
  - `predictions_sample.csv`
    - test からランダムサンプル（例: 100,000 行）
    - `race_id`, `date`, `target_sec`, `pred_sec`, `abs_error` を含む
  - `residuals_by_month.csv`
    - 月次の誤差集計（mean/median/std/RMSE/MAE/件数）
  - `residuals_by_place.csv`
    - 競艇場別の誤差集計（mean/median/std/RMSE/MAE/件数）
  - `error_quantiles.json`
    - 絶対誤差の分位点（p50/p90/p95/p99）
  - `feature_importance_gain_top100.csv`
    - gain 重要度の上位 100
  - `plots/`（可視化を充実させる）
    - `learning_curve.png`
      - valid rmse の推移（LightGBM eval のログを可視化）
    - `pred_vs_true.png`
      - 予測 vs 実測の散布図（**サンプル**）
    - `pred_vs_true_hexbin.png`
      - 密度が高い部分が見える hexbin 版（**サンプル**）
    - `residual_hist.png`
      - 残差分布（pred - true）のヒストグラム
    - `abs_error_hist.png`
      - 絶対誤差のヒストグラム
    - `residual_by_month.png`
      - 月次平均残差（線グラフ）
    - `mae_by_month.png`
      - 月次 MAE（線グラフ）
    - `rmse_by_month.png`
      - 月次 RMSE（線グラフ）
    - `error_by_place_topN.png`
      - 場別 MAE/RMSE（上位 N の棒グラフ）
    - `feature_importance_gain_top50.png`
      - gain 重要度の上位 50 を棒グラフ化
    - `feature_importance_split_top50.png`
      - split 重要度の上位 50 を棒グラフ化
    - `calibration_like_plot.png`（任意）
      - 予測値帯ごとの平均実測（バイアス確認）
    - `error_quantile_curve.png`（任意）
      - 絶対誤差分位点（p50/p90/p95/p99）

## 11. 実装内容（実際の処理）
### 11.1 実装ファイル
- 学習スクリプト: `Simulation/train_racetime_lgbm.py`
- 依存: `requirements.txt` に `lightgbm`, `scikit-learn`, `matplotlib` を追加

### 11.2 主要処理フロー（実装済み）
1. `Data/df.csv` を読み込み
2. `date` を `datetime` 化、`target_sec` を生成
3. 目的変数・日付欠損を除外
4. 任意で `year/month/dayofweek` を追加（既定: ON）
5. カテゴリ列を `category` 型に変換
6. 中間成果物を出力（8章）
7. `race_id` 単位で時系列分割（train/valid/test）
8. split の重複チェックとサンプル出力
9. LightGBM 学習（early stopping）
10. Test 評価・重要度出力・最終成果物（CSV/図）保存

### 11.3 実行方法（例）
```
python Simulation/train_racetime_lgbm.py \\
  --data-path Data/df.csv \\
  --output-dir artifacts/lgbm_racetime \\
  --train-end 2022-12-31 \\
  --valid-end 2023-12-31
```

### 11.4 CLI パラメータ（主要）
- `--data-path`: df.csv のパス
- `--output-dir`: 出力先
- `--train-end`, `--valid-end`: 分割境界日
- `--sample-frac`: `race_id` 単位のサンプリング率（既定 1.0）
- `--sample-rows`: サンプルCSVの行数（既定 10,000）
- `--pred-sample-rows`: 予測サンプルCSVの行数（既定 100,000）
- `--plot-sample-rows`: 可視化用サンプル行数（既定 200,000）
- `--error-topn-place`: 場別誤差プロットの Top-N（既定 20）
- `--no-date-features`: 日付派生特徴量を無効化

### 11.5 重要な実装上の注意
- `race_id` の split 混在を防ぐため **レース単位** で分割
- 事後情報（`着`, `決まり手`, `PST`, `TSC`）は特徴量から除外
- JSONL ログに主要イベントを記録

## 12. 実装計画（概要・実施済み）
1. `Data/df.csv` を読み込み
2. 列の型変換・欠損行削除
3. 日付でソートし分割
4. 中間成果物を出力（8章）
5. LightGBM 学習（valid で early stopping）
6. Test 評価・重要度出力

## 13. 非対象
- ハイパーパラメータの最適化や自動チューニングの詳細検討
- 目的変数を「着順」や「3連単確率」にする実験
